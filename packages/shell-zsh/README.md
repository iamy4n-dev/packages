---
name: "shell-zsh"
display_name: "Zsh Shell"
description: "Installs zsh and applies a base .zshrc config with history and completion settings."
status: stable
tags: [shell, zsh, config]
tools: [zsh]
---

# shell-zsh

Installs zsh and sets it as the default shell. Applies `.zshrc` from `.devcontainer/` if materialised by dpod-seed.
